//Zastosowano flagi:  -Wall -pedantic -Wextra -Werror -std=c99
#include <time.h>
#include <stdio.h>
#define SIZE 3
#define RAND_MAX 10

void matrixFiller(int matrix[][]);
void matrixPrinetr(int matrix[][]);
int matrixDet(int matrix[][]);


int main(void){

    int matrix[SIZE][SIZE];
    
    srand(time(NULL));
    
    matrixFiller(matrix);
    printf("Wylosowana macierz to: ");
    matrixPrinter(matrix);
    printf("Wyznacznik tej macierzy to: %d", matrixDet(matrix));
    
    return 0;
}

void matrixFiller(int matrix[][]){
    
    for(int i = 0; i <3;++i){
        for (int j = 0; j<3; ++j){
                matrix[i][j] = rand();
        }
    }
}

void matrixPrinter(int matrix[][]){

    for(int i = 0; i <3;++i){
        for (int j = 0; j<3; ++j){
                printf("%d",matrix[i][j]);
        }
        printf("\n");
    }
}

int matrixDet(int matrix[][]){
    int determinant = 0;
    for(int i = 0;i <3; )

    return determinant;
}